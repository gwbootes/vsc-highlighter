{
  "name": "tag-tint",
  "displayName": "Tag Tint",
  "description": "Tints HTML and CSS lines by structure, so an opening tag/brace and its matching closer share a line color and nesting shades darker. Built for tracking where blocks start and end.",
  "version": "0.1.0",
  "publisher": "reef",
  "engines": {
    "vscode": "^1.75.0"
  },
  "categories": [
    "Visualization"
  ],
  "activationEvents": [
    "onLanguage:html",
    "onLanguage:css",
    "onLanguage:scss",
    "onLanguage:less"
  ],
  "main": "./extension.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/gwbootes/vsc-highlighter.git"
  },
  "contributes": {
    "configuration": {
      "title": "Tag Tint",
      "properties": {
        "tagTint.opacity": {
          "type": "number",
          "default": 0.12,
          "minimum": 0,
          "maximum": 1,
          "description": "How strong the line tint is (0 = invisible, 1 = solid). Light backgrounds usually want a low value."
        }
      }
    }
  }
}
